Problem Statement
Write a python program to store roll numbers of student in array who
attended training program in random order. Write function for searching
whether particular student attended training program or not,
using Linear search and Sentinel search.

Code
def accept_array(A):
    n = int(input("enter total no. of students: "))
    for i in range(n):
        x = int(input("enter the roll no of student %d: " % (i + 1)))
        A.append(x)
    print("Student info accepted successfully\n\n")
    return n

def display_array(A, n):
    if n == 0:
        print("\nNo records in the database")
    else:
        print("Students Array:", end='  ')
        for i in range(n):
            print("%d" % A[i], end='  ')
        print("\n")

def linear_search(A, n, X):
    for i in range(n):
        if A[i] == X:
            return i
    return -1  # If element is not found

def sentinel_search(A, n, X):
    last = A[n - 1]
    i = 0
    A[n - 1] = X
    while A[i] != X:
        i += 1
    A[n - 1] = last
    if (i < n - 1) or (X == A[n - 1]):
        return i
    else:
        return -1  # If element is not found

def Main():
    A = []
    n = 0  # Initialize n to avoid errors in case search is performed before accepting input
    while True:
        print("\t 1: Accept and display student info")
        print("\t 2: Linear search")
        print("\t 3: Sentinel search")
        print("\t 4: Exit")
        ch = int(input("Enter your choice: "))
        
        if ch == 4:
            print("End of programme")
            quit()
        elif ch == 1:
            A = []  # Initialize new array
            n = accept_array(A)
            display_array(A, n)
        elif ch == 2:
            if n == 0:
                print("\nNo records available. Please enter student data first.\n")
            else:
                X = int(input("Enter roll_no to be searched: "))
                flag = linear_search(A, n, X)
                if flag == -1:
                    print("\tRoll_no not found\n")
                else:
                    print("\tRoll_no found at location %d" % (flag + 1))
        elif ch == 3:
            if n == 0:
                print("\nNo records available. Please enter student data first.\n")
            else:
                X = int(input("Enter roll_no to be searched: "))
                flag = sentinel_search(A, n, X)
                if flag == -1:
                    print("\tRoll_no not found\n")
                else:
                    print("\tRoll_no found at location %d" % (flag + 1))
        else:
            print("Wrong choice!! Try again")

Main()
